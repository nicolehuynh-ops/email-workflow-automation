[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "email-outreach-workflow"
version = "0.1.0"
description = "Local-first campaign outreach orchestration"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
